# Deepak Kumar Resume Builder

React + Vite resume page for maintaining and downloading Deepak Kumar's resume.

## Scripts

- `npm run dev` starts the local Vite dev server.
- `npm run build` creates the production build.
- `npm run lint` runs ESLint.

Resume content lives in `src/resume/deepakKumarResume.js`.
